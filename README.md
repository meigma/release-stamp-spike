# release-stamp-spike

Throwaway scratch repository for `meigma/release` architecture **spike C**: can
Release Please stamp a composite action's version without manual edits, and does
the version/protocol guard behave correctly on both acquisition paths?

Not a product. Safe to delete once the verdict is recorded in the
`meigma/release` session journal.
